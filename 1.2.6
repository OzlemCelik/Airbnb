--What's the most expensive listing? What else can you tell me about the listing?

SELECT
    *
FROM
    sfo_listings
ORDER BY
    price DESC
LIMIT 5;

Answer:

--What neighborhoods seem to be the most popular?

SELECT  sfo_listings.neighbourhood, count(*)  FROM sfo_listings GROUP BY 1 ORDER BY  2 DESC LIMIT 10


--What time of year is the cheapest time to go to your city? What about the busiest?

WITH cost AS (
    SELECT
        calender_date,
        price
    FROM
        sfo_calendar
    WHERE
        price != ''
    GROUP BY
        calender_date,
        price
)
SELECT
    price,
    calender_date
FROM
    cost
GROUP BY
    price,
    calender_date
ORDER BY
    price
LIMIT 10;
